import 'package:flutter/material.dart';

class RestoreBootstrapView extends StatelessWidget {
  const RestoreBootstrapView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
